#!/usr/bin/env node

var program = require('commander');
program
  .version('0.0.16')
  .usage('<command> [options]')
  .command('init', 'Init Project')
  .command('module','Create Module')
  .parse(process.argv);
